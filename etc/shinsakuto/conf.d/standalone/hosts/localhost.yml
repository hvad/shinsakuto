hosts:
  - id: localhost
    use: linux-server-template
    register: true
    address: 127.0.0.1
    alias: Local Machine Monitoring
    hostgroups:
      - linux-servers

services:
  - id: Ping_fake
    host_name: localhost
    check_command: echo 'Test'
    normal_interval: 60
    retry_interval: 20
    max_attempts: 3
